{{ form_start(form) }}
    {{ form_widget(form) }}
<a class="btn btn-secondary pull-right" href="{{ path('vehicle_index') }}">back to list</a>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
