{% include 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Create new rental</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success pull-right">Créer</button>
        {{ form_end(form) }}
        <a class="btn btn-secondary pull-right" href="{{ path('driver_index') }}">back to list</a>

    </div>

{% endblock body%}