{% include 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary pull-right">Créer</button>
        {{ form_end(form) }}
    </div>

{% endblock body%}